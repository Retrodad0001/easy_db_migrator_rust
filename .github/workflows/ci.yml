name: CI

on:
  push:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2

      - name: Install cargo-audit
        uses: taiki-e/install-action@7572810d7dd469b651bb7793945692cf78da5dd7 # v2
        with:
          tool: cargo-audit

      - name: Install cargo-deny
        uses: taiki-e/install-action@7572810d7dd469b651bb7793945692cf78da5dd7 # v2
        with:
          tool: cargo-deny

      - name: Run cargo audit
        run: cargo audit

      - name: Run cargo deny
        run: cargo deny check

      - name: Run Semgrep
        run: docker run --rm --entrypoint semgrep -v "${{ github.workspace }}:/src" -w /src semgrep/semgrep --config p/owasp-top-ten --config p/security-audit --config p/0xdea .

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy (both backends)
        run: cargo clippy --all-targets --features postgres,mssql -- -D warnings

      - name: Run clippy (postgres backend only)
        run: cargo clippy --all-targets --features postgres -- -D warnings

      - name: Run clippy (mssql backend only)
        run: cargo clippy --all-targets --features mssql -- -D warnings

      - name: Check that no backend fails the build
        run: |
          if cargo check 2>/dev/null; then
            echo "expected a build with no backend feature to fail, but it built" >&2
            exit 1
          fi

      - name: Build docs (both backends)
        run: cargo doc --no-deps --features postgres,mssql

      - name: Build docs (postgres backend only)
        run: cargo doc --no-deps --features postgres

      - name: Build docs (mssql backend only)
        run: cargo doc --no-deps --features mssql

      - name: Run unit tests
        run: cargo test --lib --features postgres,mssql

      - name: Run full test suite
        run: cargo test --features postgres,mssql
